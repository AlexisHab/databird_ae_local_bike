version: 2

models:
  - name: stg_brands
    description: This model contains the list of brands
    columns:
      - name: brand_id
        description: "Primary key, unique identifier for each brand."
        data_tests:
            - unique
            - not_null
      - name: brand_name
        description: "Name of the brand."

  - name: stg_categories
    description: This model contains the list of categories
    columns:
      - name: category_id
        description: "Primary key, unique identifier for each category."
        data_tests:
            - unique
            - not_null
      - name: category_name
        description: "Name of the category."

  - name: stg_customers
    description: This model contains the list of customers
    columns:
      - name: customer_id
        description: "Primary key, unique identifier for each customer."
        data_tests:
            - unique
            - not_null

  - name: stg_stores
    description: This model contains the list of stores
    columns:
      - name: store_id
        description: "Primary key, unique identifier for each store."
        data_tests:
            - unique
            - not_null
      

  - name: stg_staffs
    description: This model contains the list of staff members
    columns:
      - name: staff_id
        description: "Primary key, unique identifier for each store."
        data_tests:
            - unique
            - not_null
      - name: store_id
        description: "Foreign Key, related to the list of stores"
        data_tests:
          - relationships:
              arguments:
                to: ref('stg_stores')
                field: store_id
      - name: manager_id
        description: "List the ID of the manager, relative to staff_id."
        data_tests:
          - relationships:
              arguments:
                to: ref('stg_staffs')
                field: staff_id

  - name: stg_products
    description: This model contains the list of products
    columns:
      - name: product_id
        description: "Primary key, unique identifier for each store."
        data_tests:
          - unique
          - not_null
      - name: brand_id
        description: "Foreign Key, related to the list of brands"
        data_tests:
          - relationships:
              arguments:
                  to: ref('stg_brands')
                  field: brand_id
      - name: category_id
        description: "List the ID of the manager, relative to categories"
        data_tests:
          - relationships:
              arguments:
                  to: ref('stg_categories')
                  field: category_id